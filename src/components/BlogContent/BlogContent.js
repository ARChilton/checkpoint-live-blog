import styled from 'react-emotion'

const BlogContent = styled('div')`
  max-width: 900px;
  margin: auto;
  p,
  ul {
    font-size: 1rem;
  }

  img {
    margin: auto;
  }
`

export default BlogContent
